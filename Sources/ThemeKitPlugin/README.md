# ThemeKitPlugin

Thin command plugin entry point. Reads the config file, calls into ThemeKitGenerator, and writes the output files. Runs via Xcode ("ThemeKit Commands > Generate Theme Files") or `swift package plugin`.
